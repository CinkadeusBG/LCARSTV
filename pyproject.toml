[project]
name = "lcarstv"
version = "0.1.0"
description = "Simulates live TV channels from local media files"
requires-python = ">=3.11"
dependencies = [
    "Pillow>=10.0.0",
]

[tool.ruff]
line-length = 100

